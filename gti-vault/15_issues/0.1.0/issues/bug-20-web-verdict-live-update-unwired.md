---
issue: bug-20
title: Web verdict surface does not live-update on a reroll (web-01 Â§C cross-fade unwired)
status: done
type: AFK
github_issue: 216
created: 2026-05-22
---

> **Legacy mobile note (2026-06-05):** References to iOS, Swift, SwiftUI, TestFlight, or ios/ in this historical note refer to the retired Swift app unless explicitly stated otherwise. Active mobile app work now lives in React Native / Expo under mobile/.


# bug-20 â€” Web verdict surface does not live-update on a reroll

## Symptom

`design-system/surfaces/web-01-invitee-shell.md` Â§C ("Read-only verdict card")
specs a **live update**: "During `decided-active` the card live-updates on the
existing Realtime rebroadcast â€” if a reroll changes the verdict while the
invitee has the card open, the venue name cross-fades to the new value
(`all 320ms var(--ease-out)`)."

It does not. A web invitee holding the Â§C verdict card open during an initiator
reroll sees the card stay frozen on the old venue.

## Root cause

- `SessionRoom`'s verdict-fetch effect runs once, when `roomStatus` first flips
  to `verdict_ready`, and is guarded against re-running.
- A reroll re-runs the verdict in place. The `verdict_ready` broadcast handler
  re-sets `roomStatus` to the same `verdict_ready` value â€” React bails on the
  unchanged state, the fetch effect never re-fires, and the rendered verdict is
  never refreshed.
- No venue-name cross-fade transition is wired on the web verdict surface.

## Impact

Low and narrow â€” it affects only a web invitee who has the verdict card open
during the brief initiator-only reroll window. Outside that window there is
nothing to live-update. No data is lost; the card is simply stale until reload.

## Suggested direction (triage to confirm)

Re-fetch the verdict when a reroll produces a new one â€” e.g. key the fetch on
the verdict id / `computed_at` rather than only on the room status â€” and
cross-fade the venue name per the Â§C timing (`all 320ms var(--ease-out)`).

## Blocked by

[[bug-17-web-verdict-surface-conformance|bug-17]] â€” bug-17 rewrites the same
web verdict surface (`SessionRoom` verdict fetch, `VerdictReadOnly`,
`web/lib/verdict.ts`) to conform to Â§C. This live-update work should land on top
of that conformed surface, not race it.

## Surfaced by

Flagged during the bug-17 `/grill-with-docs` session (2026-05-22, Q4) as a
separate realtime-behavior follow-up, deliberately kept out of bug-17's
static-conformance scope.

## References

- `design-system/surfaces/web-01-invitee-shell.md` Â§C "Live update".
- `web/components/SessionRoom.tsx` â€” the verdict-fetch effect.
- `web/components/VerdictReadOnly.tsx` â€” the web verdict surface.
- [[bug-17-web-verdict-surface-conformance|bug-17]] â€” the Â§C conformance fix
  this is blocked by.

## Comments

### Triage 2026-05-22 â€” held at needs-triage, blocked by bug-17

> *This was generated by AI during triage.*

**Category:** bug. Confirmed.

**Repro â€” confirmed by code trace:**

- `SessionRoom`'s verdict-fetch effect is keyed on
  `[members.length, roomId, roomStatus]` and runs only while `roomStatus` is
  `verdict_ready` / `locked`.
- The `verdict_ready` broadcast handler calls `setRoomStatus("verdict_ready")`.
  On an in-place reroll the value is unchanged â€” React bails on the state set,
  the effect's deps do not change, the verdict is never re-fetched, and the
  card stays frozen on the old venue.
- `web-01-invitee-shell.md` Â§C ("Live update", `status: locked` 2026-05-21)
  *does* spec the cross-fade â€” `all 320ms var(--ease-out)`. This is a real
  conformance gap against a locked spec line. Impact is narrow: only a web
  invitee holding the verdict card open during the initiator-only reroll
  window.

The issue is content-complete and AFK-shaped â€” clear root cause, clear
direction, no judgment calls.

**Held at `needs-triage` â€” blocked by [[bug-17-web-verdict-surface-conformance|bug-17]] (#207).**
bug-17 (`ready-for-agent` / AFK) rewrites the exact same surface: `SessionRoom`'s
verdict effect, `VerdictReadOnly`, and `shapeVerdictView`. `/execute-issues`
only skips issues blocked by a *HITL* issue, and bug-17 is AFK â€” so promoting
bug-20 to `ready-for-agent` now would let an AFK run grab both into parallel
worktrees, and bug-20's subagent would build on the pre-conformance surface
(guaranteed conflict / wasted run).

**Flip condition:** when bug-17's PR merges, re-triage bug-20 to
`ready-for-agent` / `AFK` and write the agent brief against the conformed
(post-bug-17) `VerdictReadOnly`.

### Triage 2026-05-22 â€” ready-for-agent (AFK); flip condition met

> *This was generated by AI during triage.*

bug-17 (#207) merged via PR #219 â€” the flip condition above is satisfied.
Re-verified the root cause against the **post-bug-17** code: the `SessionRoom`
verdict-fetch effect is now keyed `[roomId, roomStatus, userId]`, and it still
never re-fires on an in-place reroll â€” the `verdict_ready` rebroadcast re-sets
`roomStatus` to its current value, React bails on the unchanged state, the
effect deps do not change, the verdict is never re-fetched. bug-17 changed the
deps but did not fix this.

**Scope correction.** The issue body claims "no venue-name cross-fade is
wired." That is now stale: bug-17's `WebVerdictCard` already wires the Â§C
cross-fade â€” a keyed re-mount (`key={verdictPlaceName}`) plus a
`gti-fade-up 320ms var(--ease-out)` animation. The fade plays automatically the
moment the rendered venue name changes. The only remaining defect is the
missing re-fetch. Status flipped to `ready-for-agent`, `type: AFK`.

#### Agent Brief

**Category:** bug
**Summary:** The web Â§C verdict card never re-fetches when an initiator reroll
changes the verdict, so it stays frozen on the stale venue.

**Current behavior:**
The web invitee Â§C read-only verdict card fetches the verdict exactly once â€”
when the room status first becomes `verdict_ready`/`locked`. The verdict-fetch
effect is keyed only on room id, room status, and user id. When an initiator
reroll re-runs the verdict in place, room status stays `verdict_ready`, so the
`verdict_ready` Realtime rebroadcast re-sets status to the same value, React
bails on the unchanged state, the effect's dependencies do not change, and the
verdict is never re-fetched. An invitee holding the card open during a reroll
sees it frozen on the old venue until a full page reload.

**Desired behavior:**
Per `web-01-invitee-shell.md` Â§C "Live update": while the Â§C verdict card is
open, every `verdict_ready` Realtime rebroadcast must re-fetch the verdict. When
the re-fetch yields a different venue â€” or flips between the default and
no-survivor variants â€” the card updates to the new value. The venue-name
cross-fade already exists in the verdict card component (a keyed re-mount + a
320ms `var(--ease-out)` animation) and plays automatically once the rendered
venue name changes; the fix is solely to make the re-fetch fire.

**Key interfaces:**
- The web session orchestrator's verdict-fetch effect â€” must re-run on every
  `verdict_ready` rebroadcast, not only on a room-status transition. A
  monotonic rebroadcast signal (e.g. a counter bumped by the broadcast handler)
  folded into the effect's inputs is one option; the agent picks the mechanism.
- The `verdict_ready` broadcast handler â€” currently only sets room status; it
  must also surface the rebroadcast as a re-fetch trigger.
- The verdict shaping helper / `VerdictView` â€” unchanged; the re-fetch flows
  through the existing shaping path. The defaultâ†”no-survivor variant swap must
  work (a reroll can turn a `no_survivor` verdict into a venue and vice versa).

**Acceptance criteria:**
- [ ] When the Â§C card is open and a `verdict_ready` rebroadcast arrives, the
      verdict is re-fetched.
- [ ] A reroll that changes the winning venue updates the card to the new venue
      name (the existing cross-fade plays).
- [ ] A reroll that flips a `no_survivor` verdict to a venue, and the reverse,
      updates the card across both Â§C variants.
- [ ] The first verdict load still fires exactly once on the initial
      `verdict_ready` â€” no redundant double-fetch on first render.
- [ ] An integration test covers open-card â†’ rebroadcast â†’ re-fetch â†’ updated
      venue, written test-first (red before green).
- [ ] `web/` `npm test`, `npm run build`, `npm run typecheck` pass; and
      `node design-system/scripts/verify.mjs` is green.

**Out of scope:**
- The venue-name cross-fade animation itself â€” already wired; do not re-implement.
- The iOS verdict surface and web/iOS verdict parity (non-goal, bug-17).
- The Waiting screen and the quiz flow.
- The verdict read shape (plan name + venue only) â€” settled by bug-17.
- The `/join/<roomId>` shell's fresh-load decided card â€” it fetches on mount,
  needs no live-update wiring.

### Done 2026-05-22 â€” merged (PR #220)

> *AFK execution closing note.*

Fixed in `web/components/SessionRoom.tsx`. The verdict-fetch effect was keyed
`[roomId, roomStatus, userId]`; an in-place reroll keeps `roomStatus` at
`verdict_ready`, so the `verdict_ready` rebroadcast re-set the status to its
current value, React bailed on the unchanged state, the effect deps did not
change, and the card stayed frozen on the stale venue.

**Fix:** added a monotonic `verdictRefetchSignal` counter. The `verdict_ready`
broadcast handler bumps it on every rebroadcast; the `rooms` UPDATE handler also
bumps it when the new status is still `verdict_ready` / `locked` (a reroll can
arrive as an UPDATE that does not move the status). The counter is folded into
the verdict-fetch effect's deps â€” a counter always changes, so the fetch
re-runs on every rebroadcast. The effect's status guard is unchanged, so the
first load still fires exactly once on the initial `verdict_ready` (the counter
starts at 0 â€” no double-fetch on mount). The re-fetch flows through the existing
`shapeVerdictView` path, so a reroll that swaps the default â†” no-survivor
variants updates the card across both. The venue-name cross-fade was already
wired in `WebVerdictCard` (bug-17) and plays automatically once the rendered
venue name changes â€” untouched.

**Tests:** new integration test
`web/components/SessionRoom.verdict-live-update.test.tsx` (test-first, RED â†’ GREEN)
covers: rebroadcast â†’ re-fetch â†’ updated venue; default â†” no-survivor variant
flips both directions; and the single-fetch-on-first-render guard. Full web
suite (152 tests), `npm run build`, `npm run typecheck`, and
`design-system/scripts/verify.mjs` all green.
