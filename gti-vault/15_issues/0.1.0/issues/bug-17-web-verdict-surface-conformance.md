---
issue: bug-17
title: Web verdict surface does not conform to locked web-01-invitee-shell Â§C
status: done
type: AFK
github_issue: 207
created: 2026-05-21
retriaged: 2026-05-22
closed: 2026-05-22
---

> **Legacy mobile note (2026-06-05):** References to iOS, Swift, SwiftUI, TestFlight, or ios/ in this historical note refer to the retired Swift app unless explicitly stated otherwise. Active mobile app work now lives in React Native / Expo under mobile/.


# bug-17 â€” Web verdict surface does not conform to locked web-01-invitee-shell Â§C

## Symptom

The web invitee verdict surface â€” `VerdictReadOnly` plus the `web/lib/verdict.ts`
shaping helpers â€” renders a full TB-15-era verdict: venue hero, a meta line, a
fixed "7:00 PM" time badge, the rule text, a row of per-member receipt chips, a
collapsible "what got cut" drawer, and a "Start a new decision" CTA.

Â§C ("Read-only verdict card", status `locked`, 2026-05-21) says the web invitee
verdict card is **plan name + verdict venue only** â€” and spells out, verbatim,
"No receipts, no per-axis cuts, no rule chip" and "No primary CTA". The surface
does not conform to its own locked spec.

## How this issue was re-scoped (triage + grill, 2026-05-22)

bug-17 was filed by an AFK-run subagent as "web verdict-read path still
references retired typed `votes` columns". A `/triage` + `/grill-with-docs`
session on 2026-05-22 found the original framing wrong on two counts:

- **The reported symptom does not reproduce.** `SessionRoom` already passes
  `votes: []` into `shapeVerdictView`. There is no live read of the dropped
  columns â€” nothing crashes, nothing renders malformed. The retired-column
  `VoteSummaryRow` type is dead code that exists only to feed receipts Â§C says
  must not render at all.
- **The suggested fix referenced a ghost.** The original direction â€” "move
  receipts onto the `verdict_for_room` RPC projection" â€” is not actionable: no
  `verdict_for_room` RPC exists anywhere in `supabase/`. Only a stale comment
  in `web/lib/mixed-platform.test.ts` claims it does.

Root cause: `VerdictReadOnly.tsx` has not been touched since TB-15 (PR #35).
tb-WF-12 ("read-only verdict card wiring") routed into the component but never
reconciled it to the Â§C spec locked on 2026-05-21. The retired-column type debt
is one symptom of that whole shaping layer being superseded.

Web/iOS verdict parity is **not** a goal â€” that was the original issue's false
premise. iOS keeps its full receipt surface; the web invitee card is, by Â§C, a
far smaller read.

## Grill decisions (2026-05-22)

- **Q1 â€” scope.** bug-17 becomes a Â§C-conformance fix for the web verdict
  surface (content + layout), not a `verdict.ts` type patch and not a receipts
  wire-up.
- **Q2 â€” no-survivor.** Â§C does not spec the `no_survivor` decided-plan case.
  bug-17 ships a minimal no-survivor variant (plan name + a "No spot fits"
  card, no votes-derived meta line); the Â§C gap is flagged as a separate
- **Q3 â€” claim-code mint line.** Â§C requires the "Getting the app?" mint line
  on the verdict card (a returning invitee of a decided plan lands on Â§C, not
  Waiting, so a Waiting-only affordance strands them). It is currently wired
  into the web Waiting screen only. bug-17 adds it to the verdict surface.
- **Q4 â€” live update.** The Â§C "Live update" reroll cross-fade is out of scope
  â€” a separate realtime behavior, flagged for its own issue.

## References

  issue conforms to ("Read-only verdict card").
- `web/lib/verdict.ts`, `web/components/VerdictReadOnly.tsx`,
  `web/components/SessionRoom.tsx` â€” the verdict-read path.
- `web/components/GettingTheAppAffordance.tsx`, `web/components/WaitingScreen.tsx`
  â€” the existing "Getting the app?" mint affordance and its Waiting-screen wiring.
- `web/lib/mixed-platform.test.ts` â€” carries the stale `verdict_for_room` RPC
  comment and the now-false verdict-view-parity assertion.
- [[tb-wf-10-web-quiz-v11-port|tb-WF-10]] â€” the write-side port; flagged this
  read path as an adjacency.
- [[0.1.0-workflow-overhaul-web-invitee-flow]] Â§Q6 â€” the decision doc behind Â§C.

## Comments

### Closed 2026-05-22 â€” Â§C-conformance fix merged (PR #219)

> *This was generated by an AFK agent.*

The web invitee verdict surface now conforms to the locked
`web-01-invitee-shell` Â§C: plan name + verdict venue only, with the Â§C
"Getting the app?" claim-code mint line. Done via an AFK run on
`afk/bug-17`.

- `web/lib/verdict.ts` â€” `VerdictView` collapsed to the Â§C shape; the
  dead retired-column shaping layer (`VoteSummaryRow`, `actionFor`,
  `survivingHardNeeds`, `metaLine`, `audienceCopy`, `displayName`, the
  `Receipt` / `Cut` / `CutRow` types and the receipts / cuts / time-badge
  / rule-text fields) removed. No retired typed `votes` column is
  referenced anywhere in the web verdict-read path.
- `web/components/VerdictReadOnly.tsx` â€” rewritten to render the Â§C
  layout by delegating to `WebVerdictCard` (the one Â§C-conformant
  component, already used by the `/join/<roomId>` shell). The
  `no_survivor` verdict renders the minimal variant â€” plan name + a
  "No spot fits" card. The Â§C "Getting the app?" mint line renders below
  the card on both variants.
- `web/components/SessionRoom.tsx` â€” the verdict load drops the
  `option_cuts` / `votes` reads and resolves the plan name through the
  joiner-readable `plans_decided_for_user` / `plans_history_for_user`
  RPCs (`readRoomPlanState`). `plans` carries a creator-only SELECT
  policy, so a web invitee cannot read `plans.name` directly â€” the
  joiner RPC is the same path the `/join` shell already uses.
- `web/lib/mixed-platform.test.ts` â€” write-side wire-shape parity
  assertion kept; the now-false verdict-view-parity assertion and the
  stale `verdict_for_room` RPC comment removed.

Verified: `npm test` (149 pass), `tsc --noEmit` clean, `next build`
verdict-bearing routes (`/join`, `/s`) serve in a real Next.js dev
server. The two grill follow-ups (bug-20 Â§C live-update, and the Â§C
no-survivor spec-gap) remain tracked separately.

### Merge note 2026-05-22 â€” AFK resume (rebase + CI flake)

> *This was generated by an AFK agent.*

The original bug-17 subagent finished implementation and opened PR #219,
then crashed mid-CI-watch (the `ios` job had hung `in_progress` >80m â€” a
GitHub macOS-runner / simulator infra flake, since cancelled). A resume
run carried the PR over the line:

- **Rebase.** `afk/bug-17` was behind `main` after sg-WF-9 (#217) merged.
  Rebased onto `origin/main` â€” clean, no conflict. sg-WF-9 added the
  no-survivor variant to `web-01-invitee-shell.md` Â§C (a spec-only edit);
  bug-17 changes only `web/` code + this vault doc and never touches that
  spec file, so the two are orthogonal. Both sides land intact â€”
  bug-17's `NO_SURVIVOR_VENUE` already equals the `"No spot fits"` string
  sg-WF-9 ratified in Â§C, so code and spec agree with no reconciliation.
- **CI flake.** The first post-rebase run failed `web` on one test â€”
  `InviteShell.reclick.test.tsx > live-updates the verdict venue on a
  Realtime rebroadcast` â€” a pre-existing tb-WF-12 test (PR #204) that
  bug-17 does not touch. It is timing-sensitive under CI parallel-suite
  load (5/5 + 3/3 green locally in isolation and full-suite). Re-ran the
  failed job; it passed (49s), and the `ios` job ran clean in 3m40s with
  no hang. Flagged as a flaky-test follow-up â€” out of scope for bug-17.

### Triage + grill 2026-05-22 â€” re-scoped, ready-for-agent (AFK)

> *This was generated by AI during triage.*

Filed as a narrow "fix `verdict.ts` retired columns" bug; a `/triage` +
`/grill-with-docs` session found the framing wrong (the symptom does not
reproduce; the suggested `verdict_for_room` RPC does not exist) and re-scoped it
to a Â§C-conformance fix. Four grill questions resolved (see Â§"Grill decisions").
Status `needs-triage` â†’ `ready-for-agent`; type `HITL` â†’ `AFK`. Two follow-ups
flagged for separate filing: the Â§C no-survivor spec-gap, and the Â§C live-update
reroll cross-fade.

#### Agent Brief

**Category:** bug

**Summary:** Conform the web invitee verdict surface to the locked
`web-01-invitee-shell.md` Â§C spec â€” plan name + venue only â€” and add the
Â§C-required "Getting the app?" claim-code mint line.

**Current behavior:**
The web invitee verdict surface renders a TB-15-era full verdict: venue hero,
meta line, a fixed time badge, rule text, per-member receipt chips, a
collapsible cuts drawer, and a "Start a new decision" CTA. `web/lib/verdict.ts`
carries a `VoteSummaryRow` type and `actionFor` / `survivingHardNeeds` helpers
typed against retired `votes` columns; `SessionRoom` already feeds the
verdict shaper an empty `votes` array, so the receipt machinery never runs on
real data. The "Getting the app?" claim-code mint affordance is wired into the
web Waiting screen only â€” not the verdict surface.

**Desired behavior:**
The web invitee verdict surface conforms to `web-01-invitee-shell.md` Â§C:

- *Default (a venue was chosen):* eyebrow, the plan name, and the verdict venue
  in a single card â€” nothing else. No receipts, no cuts drawer, no rule chip,
  no time badge, no primary CTA.
- *No-survivor (`method: no_survivor`):* the same minimal register â€” plan name
  plus a "No spot fits" card in the venue slot. No votes-derived hard-needs
  meta line (its only data source, `votes`, is ephemeral and gone by
  decided-time).
- The Â§C "Getting the app?" claim-code mint line renders below the verdict card
  on both variants, mirroring the affordance already wired into the web Waiting
  screen.
- The dead retired-column shaping layer is removed.

**Key interfaces:**

- The web verdict shaper (`shapeVerdictView` in `web/lib/verdict.ts`) â€” its
  `VerdictView` result collapses to the Â§C surface: a place / plan name, a
  no-survivor flag, and nothing receipt- or cut-derived. The `votes` input
  parameter, `VoteSummaryRow`, `actionFor`, `survivingHardNeeds`, the `Receipt`
  / `Cut` types, and the `receipts` / `cuts` fields are removed.
- The web verdict component (`VerdictReadOnly`) â€” strips the time badge, the
  rule paragraph, the receipts row, the cuts drawer, and the "Start a new
  decision" CTA; renders the Â§C layout (wordmark, eyebrow, plan name, venue
  card) and the "Getting the app?" mint line below the card.
- `SessionRoom`'s verdict phase â€” passes an `onMintClaimCode` handler to the
  verdict surface, the same way its Waiting phase already does. The verdict
  fetch no longer needs to load `option_cuts` or `votes` for the view.
- The shared `GettingTheAppAffordance` component â€” reused as-is on the verdict
  surface.

**Acceptance criteria:**

- [ ] The web verdict surface renders only: eyebrow, plan name, the verdict
      venue card, and the "Getting the app?" mint line â€” matching
      `web-01-invitee-shell.md` Â§C.
- [ ] No receipts, no cuts drawer, no rule text, no time badge, and no
      "Start a new decision" CTA appear on the web verdict surface.
- [ ] A `no_survivor` decided plan renders the minimal variant: plan name + a
      "No spot fits" card + the mint line.
- [ ] The "Getting the app?" line mints a claim code from the verdict surface,
      mirroring the web Waiting screen wiring.
- [ ] `VoteSummaryRow`, `actionFor`, `survivingHardNeeds`, and the receipt /
      cut shaping are removed from `web/lib/verdict.ts`; no retired typed
      `votes` column (`q1_vetoes`, `q2_budget`, `q3_walk_minutes`, `q4_vibe`,
      `q5_regret`) is referenced anywhere in the web verdict-read path.
- [ ] `web/lib/mixed-platform.test.ts` is reconciled â€” the write-side
      wire-shape parity assertion (web `buildVoteRow` emits the generic
      `q1`..`q5` slots) is kept; the now-false verdict-view-parity assertion
      and the stale `verdict_for_room` RPC comment are removed.
- [ ] `VerdictReadOnly`'s render test is updated to the Â§C surface.
- [ ] Web CI lane green â€” `npm run typecheck` (the `tsc --noEmit` gate added
      by bug-18 / PR #213), `npm test`, and `npm run build`. Removing the dead
      `verdict.ts` shaping layer must leave `tsc --noEmit` clean. (`web/` ships
      no ESLint config, so `npm run lint` is not a CI gate.)

**Out of scope:**

- The Â§C "Live update" reroll cross-fade â€” a separate realtime behavior,
  flagged for its own issue (grill Q4).
- Any edit to `web-01-invitee-shell.md` itself â€” Â§C is correct and locked; this
  is a code-conformance fix, not a spec change.
- The absence of a `no_survivor` case from the Â§C spec text â€” flagged as a
  minimal no-survivor variant in the meantime.
- The iOS verdict surface â€” iOS keeps its full receipt surface.
- bug-18's `tsc --noEmit` test-file type errors â€” tracked separately.
