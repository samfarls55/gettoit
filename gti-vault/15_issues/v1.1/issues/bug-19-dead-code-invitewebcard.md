---
issue: bug-19
title: Retire dead code web/components/InviteWebCard.tsx
status: ready-for-agent
type: AFK
github_issue: 209
created: 2026-05-21
---

# bug-19 — Dead component: web/components/InviteWebCard.tsx

## Symptom

`web/components/InviteWebCard.tsx` is unreferenced production code — its only
importer is its own test, `web/components/InviteWebCard.test.tsx`. No live route
or component renders it.

## Detail

- The web invitee shell work (tb-WF-11 / tb-WF-12) replaced the old invite-card
  surface; `InviteWebCard` was left behind.
- tb-WF-11 initially flagged both `InviteWebCard` and the `/s/[sessionId]` route
  as dead. tb-WF-12 corrected that — the `/s/[sessionId]` route is **live** (it
  still hosts `SessionRoom`). Only `InviteWebCard` is genuinely dead.

## Suggested direction (triage to confirm)

Delete `web/components/InviteWebCard.tsx` and its test
`web/components/InviteWebCard.test.tsx`. Confirm no other importer first.

## Impact

None functionally — pure cleanup. Dead code carries maintenance and
comprehension cost.

## Surfaced by

Flagged by the tb-WF-12 subagent during the 2026-05-21-1812 AFK execution run.

## References

- `web/components/InviteWebCard.tsx`, `web/components/InviteWebCard.test.tsx`

## Comments

### Triage 2026-05-22 — ready-for-agent (AFK)

> *This was generated by AI during triage.*

Reproduced: `grep` across `web/app`, `web/components`, `web/lib` confirms the only importer of `InviteWebCard` is its own `InviteWebCard.test.tsx`. No live route renders it. Type flipped HITL -> AFK — pure deletion, no human judgment.

#### Agent Brief

**Category:** bug
**Summary:** Delete the dead `InviteWebCard` component and its test.

**Current behavior:**
The `InviteWebCard` web component is unreferenced production code — its sole importer is its own render test. No live route or component renders it. The web invitee shell work (tb-WF-11 / tb-WF-12) replaced the surface it served and left it behind.

**Desired behavior:**
The dead component and its test are removed from the repo. The web build, lint, and test suite stay green.

**Acceptance criteria:**
- [ ] The `InviteWebCard` component file is deleted.
- [ ] The `InviteWebCard` render-test file is deleted.
- [ ] No importer of `InviteWebCard` remains anywhere under `web/`.
- [ ] `npm test`, `npm run build`, and `npm run lint` in `web/` all pass.

**Out of scope:**
- The `/s/[sessionId]` route — confirmed live (it hosts `SessionRoom`); do not touch it.
- Any other web component cleanup.
