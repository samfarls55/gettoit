---
issue: bug-18
title: tsc --noEmit type error in web/lib/quiz.test.ts is not CI-gated
status: ready-for-agent
type: AFK
github_issue: 208
created: 2026-05-21
---

# bug-18 — Uncaught `tsc --noEmit` type error in web/lib/quiz.test.ts

## Symptom

`web/lib/quiz.test.ts` lines 128-129 produce a `tsc --noEmit` type error. CI
does not catch it: the web lane runs only `npm test` (Vitest) and `npm run
build` (`next build`) — neither does a standalone `tsc --noEmit` of the test
files, so the error ships silently green.

## Detail

- Lines 128-129 are two `(row as Record<string, unknown>)` casts asserting the
  retired v1 `votes` columns (`q1_vetoes`, `q3_walk_minutes`) do not leak onto
  the new generic-slot row.
- Left behind by the tb-WF-10 web quiz v1.1 port.
- Two sub-problems for triage to scope:
  1. Fix the type error itself.
  2. Decide whether the web CI lane should gain a `tsc --noEmit` gate so this
     class of error cannot ship green again.

## Impact

Low — a test-file-only type error; the runtime tests pass. But it is invisible
to CI, so it will rot and can mask future type regressions in the test suite.

## Surfaced by

Flagged by the tb-WF-11 subagent during the 2026-05-21-1812 AFK execution run
(noted as pre-existing, not introduced by tb-WF-11).

## References

- `web/lib/quiz.test.ts:128-129`
- `.github/workflows/ci.yml` — the web lane.

## Comments

### Triage 2026-05-22 — ready-for-agent (AFK)

> *This was generated by AI during triage.*

Reproduced via `npx tsc --noEmit` in `web/`. **Scope is wider than the issue documents** — the run surfaces errors in two files, not one:

- `quiz.test.ts` — two `TS2352` casts of a `VoteRow` to `Record<string, unknown>` (the cast must route through `unknown`).
- `claim-code.test.ts` — a possibly-`undefined` `client` (`TS18048`) and a `FunctionsClient` -> mock cast (`TS2352`).

The CI-gate sub-decision is resolved: **yes, add a `tsc --noEmit` gate to the web lane.** Type flipped HITL -> AFK. Brief covers both files + the gate.

#### Agent Brief

**Category:** bug
**Summary:** Fix the uncaught `tsc --noEmit` errors across the web test suite and add a CI gate so the class cannot ship green again.

**Current behavior:**
The web CI lane runs only `npm test` (Vitest) and `npm run build` (`next build`). Neither type-checks the test files, so `tsc --noEmit` errors in the test suite ship silently green. Two test files currently fail `tsc --noEmit`: the quiz test (casts of a vote row through `Record<string, unknown>`) and the claim-code test (a possibly-undefined client and a functions-client-to-mock cast).

**Desired behavior:**
`tsc --noEmit` over the whole `web/` project — test files included — passes clean. The web CI lane runs `tsc --noEmit` and fails the build on any type error.

**Key interfaces:**
- The web `tsconfig.json` — already includes test files in the type-check program (that is why the errors surface); confirm, do not narrow it.
- The web lane in `.github/workflows/ci.yml` — gains a `tsc --noEmit` step. Per repo convention, CI changes go as a step/job in the single `ci.yml`, never a separate workflow file.

**Acceptance criteria:**
- [ ] `npx tsc --noEmit` in `web/` exits 0 with no errors.
- [ ] The quiz-test and claim-code-test type errors are both fixed without weakening the assertions those tests make.
- [ ] The web CI lane runs `tsc --noEmit` and fails on any type error.
- [ ] `npm test`, `npm run build`, and `npm run lint` in `web/` still pass.

**Out of scope:**
- Changing the runtime intent of any test assertion.
- Type errors outside the `web/` project.
- The retired-`votes`-column read path — tracked separately in bug-17.
