---
issue: sg-WF-9
title: web-01-invitee-shell §C does not spec the no-survivor decided-plan case
status: ready-for-agent
type: AFK
feature: workflow-overhaul
github_issue: 215
created: 2026-05-22
triaged: 2026-05-22
---

# SG-WF-9 — web-01 §C does not spec the no-survivor verdict card

## Symptom

`design-system/surfaces/web-01-invitee-shell.md` §C ("Read-only verdict card")
specs the web invitee verdict card as **plan name + verdict venue only**. It
does not address a decided plan whose verdict is `method: no_survivor` — a
verdict row exists, the plan *is* decided, but there is no winning venue to put
in the card's venue slot.

§D ("This plan is closed") is a different case — it covers a membership that
does **not** resolve (a purged member row, or a stranger with no member row). A
no-survivor plan has a resolving membership and a real verdict; by §B's resume
routing ("Plan decided -> §C") it lands on §C, which has no no-survivor variant.

## Detail

- A `no_survivor` verdict is a legitimate, reachable outcome — the engine
  writes one whenever no candidate survives the combined constraints.
- The §C layout (wordmark -> eyebrow -> plan name -> verdict card -> auto) and
  copy register assume a venue. There is no spec for what the card says when
  there is none.
- The iOS S05 verdict surface has a no-survivor mode; §C, the web invitee's
  far-smaller analogue, was specced for the venue case only.

## Interim state (bug-17)

[[bug-17-web-verdict-surface-conformance|bug-17]] — the web verdict §C
conformance fix — ships a **minimal interim** no-survivor variant so the web
path does not regress: plan name + a "No spot fits" card in the venue slot, no
votes-derived meta line (`votes` is ephemeral and gone by decided-time). This
spec-gap is to ratify or replace that interim treatment in §C proper.

## Suggested direction (triage to confirm)

Amend `web-01-invitee-shell.md` §C with a no-survivor variant: the same minimal
register (plan name + a single card), the card's copy ("No spot fits" or a
chosen alternative), the eyebrow, and whether any body copy is warranted. Keep
it consistent with §C's "this is a read, not the full S05 Verdict surface"
posture and the locked Sunset Pop copy register. `verify.mjs` must stay green.

## Surfaced by

Flagged during the bug-17 `/grill-with-docs` session (2026-05-22, Q2) as a
design-system spec-gap follow-up.

## References

- `design-system/surfaces/web-01-invitee-shell.md` §C — the locked spec with
  the gap.
- [[bug-17-web-verdict-surface-conformance|bug-17]] — ships the interim
  no-survivor variant this spec-gap ratifies.
- [[workflow-overhaul-web-invitee-flow]] §Q6 — the decision doc behind §C.

## Comments

### Triage 2026-05-22 — ready-for-agent (AFK)

> *This was generated by AI during triage.*

Filed 2026-05-22 as a `needs-triage` follow-up from the bug-17
`/grill-with-docs` session. A `/triage` pass confirmed the gap against the
locked spec: §C of `web-01-invitee-shell.md` specs the read-only verdict card
as plan name + verdict venue only — the §C layout table assumes a venue name
and there is no `no_survivor` variant, though a decided no-survivor plan is a
real, reachable state that §B resume routing lands on §C. Status
`needs-triage` → `ready-for-agent`; type set to `AFK` (maintainer directed —
the variant ratifies bug-17's interim treatment, an in-register copy choice the
agent carries with full autonomy). No grill needed; the suggested direction was
already complete and the one open fork — AFK vs HITL — was the maintainer's to
call.

#### Agent Brief

**Category:** spec-gap — design-system surface amendment.

**Summary:** Amend `design-system/surfaces/web-01-invitee-shell.md` §C
("Read-only verdict card") with a `no_survivor` variant, codifying the interim
no-survivor treatment bug-17 ships in web code into the locked §C spec.

**Current behavior:**
§C of `web-01-invitee-shell.md` (`status: locked`, 2026-05-21) specs the web
invitee read-only verdict card as plan name + verdict venue only. Its layout
table and copy register assume a winning venue exists. It does not address a
decided plan whose verdict carries `method: no_survivor` — a real, reachable
engine outcome, written whenever no candidate survives the combined
constraints. A no-survivor plan has a resolving membership and a real verdict
row, so §B's resume routing ("Plan decided → §C") lands it on §C, which has no
no-survivor variant. The card's venue slot has nothing specced to put in it.

**Desired behavior:**
§C gains an explicit no-survivor variant, in the same minimal register as the
venue case — this is a read, not the full S05 Verdict surface. The variant
codifies the interim treatment bug-17 (#207) ships in code so the spec and the
web code describe the same surface regardless of which merges first:

- Same layout skeleton — wordmark → eyebrow → plan name → a single card.
- The card carries a "No spot fits" line (or a chosen equivalent consistent
  with the locked Sunset Pop copy register and the iOS S05 no-survivor mode) in
  place of a venue name.
- No votes-derived meta / hard-needs line — `votes` is ephemeral and gone by
  decided-time, the same rule §C's venue case already states.
- Decide the eyebrow: whether `"Tonight's verdict"` still fits a no-survivor
  outcome or whether the variant needs its own eyebrow. Keep it consistent with
  §C's register and the iOS S05 no-survivor surface.
- No body copy unless the register clearly warrants it — §C's venue case
  deliberately carries none.
- No primary CTA — §C is CTA-less and the no-survivor variant inherits that;
  the low-key "Getting the app?" mint line still renders below the card.

**Key interfaces:**

- `design-system/surfaces/web-01-invitee-shell.md` §C — the no-survivor variant
  is added here: a layout note / sub-table for the no-survivor case, the card
  copy, the eyebrow decision, and the copy-register entry. The doc is
  `status: locked`; this is a deliberate amendment to a locked surface — follow
  `design-system/CLAUDE.md` for how the `locked-date` is handled.
- `design-system/CHANGELOG.md` — a one-line entry (date, what, why) for the §C
  amendment, per `design-system/CLAUDE.md`.

**Acceptance criteria:**

- [ ] §C of `web-01-invitee-shell.md` specs a `no_survivor` variant — the
      layout, the card copy, the eyebrow, and whether body copy applies.
- [ ] The variant matches bug-17's interim treatment: plan name + a single
      "No spot fits"-style card, no votes-derived meta line.
- [ ] The variant explicitly states no primary CTA and retains the "Getting
      the app?" mint line, consistent with §C's venue case.
- [ ] `design-system/CHANGELOG.md` carries a one-line entry for the amendment.
- [ ] `node design-system/scripts/verify.mjs` passes — including the web-01
      presence / completeness check.

**Out of scope:**

- The web code that renders §C — bug-17 (#207) owns the code conformance and
  ships the interim variant this spec-gap ratifies. This issue is the
  spec-doc amendment only.
- The iOS S05 verdict surface — it already has a no-survivor mode; reference it
  for register, do not change it.
- The §C "Live update" reroll cross-fade — a separate web-code follow-up
  ([[bug-20-web-verdict-live-update-unwired|bug-20]]).
- Any change to §A / §B / §D / §E of `web-01-invitee-shell.md`.
